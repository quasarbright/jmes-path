name:                jmes-path
version:             0.1.0.0
github:              "quasarbright/jmes-path"
license:             BSD3
author:              "mdelmonaco"
maintainer:          "mdelmonacochs@gmail.com"
copyright:           "Mike Delmonaco 2021"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/quasarbright/jmes-path#readme>

dependencies:
- base >= 4.7 && < 5
- containers
- megaparsec
- mtl
- extra
- transformers
- lens
- aeson
- text
- vector
- unordered-containers

ghc-options:
- -w
- -Wall

library:
  source-dirs: src
  default-extensions:
  - LambdaCase
  - RecordWildCards
  - GeneralizedNewtypeDeriving
  - TupleSections
  - MonadComprehensions
  - MultiWayIf


executables:
  jmes-path-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    - -w
    - -Wall
    dependencies:
    - jmes-path
    default-extensions:
    - LambdaCase
    - RecordWildCards
    - GeneralizedNewtypeDeriving
    - TupleSections
    - MonadComprehensions
    - MultiWayIf

tests:
  jmes-path-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    - -w
    - -Wall
    dependencies:
    - jmes-path
    - hspec
    - QuickCheck
    - HUnit
    default-extensions:
    - LambdaCase
    - RecordWildCards
    - GeneralizedNewtypeDeriving
    - TupleSections
    - MonadComprehensions
    - MultiWayIf
